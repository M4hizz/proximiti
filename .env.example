# ==============================================
# DEPLOYMENT GUIDE
# ==============================================
# FRONTEND  → Vercel  (set VITE_* vars in Vercel dashboard)
# BACKEND   → Render  (set non-VITE vars in Render dashboard)
#
# Vercel env vars needed:
#   VITE_API_URL              → https://<your-render-service>.onrender.com/api
#   VITE_GOOGLE_CLIENT_ID     → Google OAuth client ID
#   VITE_GOOGLE_PLACES_API_KEY
#   VITE_STRIPE_PUBLISHABLE_KEY
#
# Render env vars needed:
#   DATABASE_URL              → libsql://your-db-name.turso.io  (from Turso)
#   DATABASE_AUTH_TOKEN       → <your-turso-auth-token>         (from Turso)
#   FRONTEND_URL              → https://<your-vercel-app>.vercel.app
#   JWT_SECRET, COOKIE_SECRET, SESSION_SECRET
#   GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET
#   STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_ID
#   GOOGLE_PLACES_API_KEY, SERPAPI_KEY, GEMINI_API_KEY
# ==============================================

# ==============================================
# SERVER CONFIGURATION
# ==============================================
# Port for the Express server
PORT=3001

# Environment mode (development | production)
NODE_ENV=development

# ==============================================
# DATABASE CONFIGURATION
# ==============================================
# SQLite database file path (relative or absolute)
DATABASE_PATH=./database.sqlite

# ==============================================
# JWT (JSON Web Token) CONFIGURATION
# ==============================================
# Secret key for signing JWT tokens
# IMPORTANT: Generate a strong random string for production!
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-key-change-in-production

# JWT access token expiration time (e.g., 60s, 5m, 1h, 7d)
JWT_EXPIRES_IN=7d

# JWT refresh token expiration time
JWT_REFRESH_EXPIRES_IN=30d

# ==============================================
# GOOGLE OAUTH CONFIGURATION
# ==============================================
# Get these from: https://console.cloud.google.com/apis/credentials
# Required for Google Sign-In functionality
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Frontend Google OAuth (must match GOOGLE_CLIENT_ID)
# VITE_ prefix makes it available to the frontend
VITE_GOOGLE_CLIENT_ID=your-google-client-id

# ==============================================
# COOKIE & SESSION CONFIGURATION
# ==============================================
# Secret for signing cookies
# IMPORTANT: Generate a strong random string for production!
COOKIE_SECRET=your-super-secure-cookie-secret-change-in-production

# Cookie domain (optional, for production)
# Example: .yourdomain.com (include the dot for subdomains)
# COOKIE_DOMAIN=

# Session secret for additional security
# IMPORTANT: Generate a strong random string for production!
SESSION_SECRET=your-super-secure-session-secret-change-in-production

# ==============================================
# SECURITY CONFIGURATION
# ==============================================
# Number of bcrypt salt rounds for password hashing (10-12 recommended)
BCRYPT_ROUNDS=12

# ==============================================
# CORS CONFIGURATION
# ==============================================
# Frontend URL for CORS (production deployment URL)
# For local development, this should be your Vite dev server
FRONTEND_URL=http://localhost:5173

# ==============================================
# API CONFIGURATION (Frontend)
# ==============================================
# Backend API URL for frontend to connect to
# VITE_ prefix makes it available to the frontend
VITE_API_URL=http://localhost:3001/api

# ==============================================
# GOOGLE PLACES CONFIGURATION
# ==============================================
# Get from: https://console.cloud.google.com → APIs & Services → Credentials
# Restrict VITE_ key to HTTP referrers in Google Console.
VITE_GOOGLE_PLACES_API_KEY=your-google-places-api-key
GOOGLE_PLACES_API_KEY=your-google-places-api-key

# ==============================================
# SERPAPI CONFIGURATION (optional)
# ==============================================
# Get from: https://serpapi.com (100 searches/month free)
# Without this key, Google reviews are capped at 5 (Places API limit).
SERPAPI_KEY=your-serpapi-key

# ==============================================
# GEMINI AI CONFIGURATION
# ==============================================
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# ==============================================
# STRIPE CONFIGURATION (Premium Subscriptions)
# ==============================================
# Get from: https://dashboard.stripe.com/apikeys
# Use sk_test_... for test mode, sk_live_... for production
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key

# Stripe publishable key (used by the frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key

# Stripe webhook secret — get from Stripe Dashboard > Webhooks
# Used to verify webhook events
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# Stripe Price ID for the Premium subscription plan
# Create a product + recurring price in Stripe Dashboard, then paste the price_... ID here
STRIPE_PRICE_ID=price_your-premium-price-id

# NOTE: For hackathon demo, leave STRIPE keys empty and use the
# "Instant Demo Upgrade" button in the app. No real payment needed.
